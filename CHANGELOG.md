# Changelog

## 0.3.0

- 重写 README，突出真实单用户 OpenClaw 安全实践和差异化能力；
- 公开当前完整脱敏通用源码，不再依赖 Base64 归档恢复；
- 新增默认只预览、显式应用、配置备份和失败恢复的 `install.sh`；
- 新增安装器隔离自测和当前 66 项运行时回归；
- 增加环境变量化公开配置层，移除生产路径、账号和 Cron 身份；
- 默认发布验证检查当前树，深度 Git 对象检查改为显式 `--history`；
- 更新插件 Schema，使其与当前透明 Operation Bus 运行时一致。
